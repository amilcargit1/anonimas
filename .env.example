# URL de conexión a PostgreSQL (Render Postgres, Neon, Supabase, etc.)
DATABASE_URL=postgresql://usuario:password@host:5432/nombre_db

# Contraseña para entrar al panel /admin.html
ADMIN_PASSWORD=cambia-esta-clave

# Puerto (Render lo asigna solo, esto es solo para local)
PORT=3000
